import { ScrollSection } from '../ScrollSection';
import { Services } from '../Services';
import {Token} from '../Token';

export const Main = () => {
  return (
    <>
      <Token />

      <ScrollSection />

      <Services />
    </>
  );
};
